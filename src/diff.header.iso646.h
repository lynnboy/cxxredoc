[section#diff.header.iso646.h
    [:en] Header [`<iso646.h>]
]

[%@library <iso646.h> [sortas iso646.h]]

[para]
[:en] The tokens [`and], [`and_eq], [`bitand], [`bitor], [`compl], [`not_eq],
[`not], [`or], [`or_eq], [`xor], and [`xor_eq] are keywords in this
International Standard ([#lex.key]).
[:en] They do not appear as macro names defined in
[%@library <ciso646> [sortas ciso646]] [`<ciso646>].
