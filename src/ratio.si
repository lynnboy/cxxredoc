[section#ratio.si
    [:en] SI types for [`ratio]
]

[para]
[:en]
For each of the typedefs [`yocto], [`zepto], [`zetta], and [`yotta], if both of
the constants used in its specification are representable by [`intmax_t], the
typedef shall be defined; if either of the constants is not representable by
[`intmax_t], the typedef shall not be defined.
